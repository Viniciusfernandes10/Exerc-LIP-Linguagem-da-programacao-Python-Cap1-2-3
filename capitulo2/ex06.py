def recebe_coordenadas(x, y):
    coordenadas = (x, y)
    return coordenadas

x = float(input())
y = float(input())

coordenadas = recebe_coordenadas(x, y)

print("Coordenadas:", coordenadas)